# Copy this to terraform.tfvars and fill in your values
aws_region   = "us-east-1"
ssh_key_name = "your-key-pair-name"   # An existing EC2 key pair
my_ip        = "0.0.0.0/0"           # Your IP for SSH, e.g. "203.0.113.50/32"
